import { SortInAlphabeticalOrderPipe } from './sort-in-alphabetical-order.pipe';

describe('SortInAlphabeticalOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortInAlphabeticalOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
